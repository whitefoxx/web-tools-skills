{
  "name": "web-tools-skills",
  "version": "0.1.0",
  "description": "web-tools \u2014 drive your logged-in Chrome from any external AI agent (Claude Code, Codex, localmd) via WebCLI or localmd Connect. Shared transport daemon + agent skills (driving guides + adapter skills).",
  "type": "module",
  "bin": {
    "web-tools-bridge": "server.mjs"
  },
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
